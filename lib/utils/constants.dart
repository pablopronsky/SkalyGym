import 'package:flutter/material.dart';

class AppColors {
  static const backgroundColor = Color.fromRGBO(57, 66, 75, 1);
  static const errorColor = Colors.red;
  static const successColor = Colors.greenAccent;
  static const accentColor = Color.fromRGBO(255, 137, 59, 1.0);
  static const textFieldColor = Color.fromRGBO(70, 84, 97, 1.0);
  static const fontColor = Colors.white;
  static const fontLinkColor = Colors.blue;
  static const blackColor = Colors.black;
}
